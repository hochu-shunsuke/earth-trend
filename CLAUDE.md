# earth-trend — 検索トレンド可視化「検索の宇宙」(仮称)

earth-trend.com(本番公開済み)。検索の普遍的注意を可視化するNext.jsアプリ。
旧 `bizz` モノレポの `cosmos/` を独立リポジトリに昇格したもの(kikaku-tools=旧`app/`は破棄)。

- `src/` — アプリ本体(Next.js App Router)
- `scripts/` — 翻訳ウォーミング等のバッチ
- `docs/` — 意思決定記録(DECISIONS.md)・設計方針(ARCHITECTURE.md)・バックログ・哲学(PHILOSOPHY.md)

## 開発ルール

### デザインは「デザインフェーズ」まで書かない(オーナーの教訓)
AI開発は最初から見た目を作り込みがちだが、それは負債になる(トークンを消費し、後で確実に書き直される)。
機能が固まるまでは素のHTML+最小限のクラスで書く。装飾・アニメーション・余白調整・配色設計は、オーナーが「デザインフェーズに入る」と明言するまで一切やらない。
機能上必要な見た目(例: グラフ可視化の背景色)だけは例外。

### モデルの使い分け(コスト方針・2026-06-13改訂)
- **複雑なこと(設計・実装・デバッグ)**: メインセッション(Fable)が直接行うか、委譲する場合も `model: "opus"`
- **単純作業のみ** `model: "sonnet"` に委譲してよい: コマンド実行の代行・md編集・機械的な置換など、judgmentが不要なもの
- 改訂理由: Sonnetに委譲したCanvas実装でクリック判定の欠落等が発生し、検収・修正コストが委譲の節約分を上回ったため
- サブエージェントはコールドスタートなので、小さい作業はそもそも委譲しない

### その他
- パッケージマネージャはpnpm
- 収益化コード(広告等)は指示があるまで入れない
- 外部データはサーバー側でキャッシュして礼儀正しく叩く(コストほぼゼロ設計)
- コミットは日本語で簡潔に
