# bizz — 個人事業モノレポ

- `app/` — kikaku-tools.com(規格ツールズ)。パーク中。触る前に `docs/DECISIONS.md` を読む
- `cosmos/` — 検索トレンド可視化「検索の宇宙」(仮称)。現在の本命
- `docs/` — 意思決定記録(DECISIONS.md)・設計方針(ARCHITECTURE.md)・バックログ

## 開発ルール

### デザインは「デザインフェーズ」まで書かない(オーナーの教訓)
AI開発は最初から見た目を作り込みがちだが、それは負債になる(トークンを消費し、後で確実に書き直される)。
機能が固まるまでは素のHTML+最小限のクラスで書く。装飾・アニメーション・余白調整・配色設計は、オーナーが「デザインフェーズに入る」と明言するまで一切やらない。
機能上必要な見た目(例: グラフ可視化の背景色)だけは例外。

### モデルの使い分け(コスト方針)
メインセッション(最高モデル)は戦略・アーキテクチャ判断・レビュー・統合に使う。
仕様が固まった自己完結の実装タスクは、Agentツールで `model: "sonnet"` のサブエージェントに委譲する(コンポーネント実装・定型ルート・テスト等)。
判断を含む複雑な実装のみ `model: "opus"`。
ただしサブエージェントはコールドスタートなので、数行の修正など小さい作業は委譲せずメインが直接行う(委譲の仕様書き出しコストの方が高い)。

### その他
- パッケージマネージャはpnpm
- 収益化コード(広告等)は指示があるまで入れない
- 外部データはサーバー側でキャッシュして礼儀正しく叩く(コストほぼゼロ設計)
- コミットは日本語で簡潔に
